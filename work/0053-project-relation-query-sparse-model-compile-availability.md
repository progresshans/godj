---
id: GDJ-0053
status: active
updated: 2026-08-31
baseline_branch: "feature/pre-release-compatibility-reset"
baseline_commit: "2273df6d8f0d7c41a24dc65ba49b42e1be0885b8"
depends_on: ["GDJ-0025", "GDJ-0026", "GDJ-0036", "GDJ-0037", "GDJ-0048"]
contracts: ["GEN-M1-001", "Q-017"]
allowed_paths:
  - "codegen/project_relation_query.go"
  - "codegen/project_relation_query_test.go"
  - "codegen/project_bundle_test.go"
  - "docs/status/CURRENT.md"
  - "docs/status/TEST_EVIDENCE.md"
  - "work/0053-project-relation-query-sparse-model-compile-availability.md"
  - "work/README.md"
integration_owner: "one primary agent"
---

# GDJ-0053 — Project Relation-query Sparse-model Compile Availability

## 사용자에게 보이는 결과

현재 public Schema/ProjectSpec만 사용한 프로젝트에 required ForeignKey relation-query source와 함께 relation-query surface가
없는 scalar-only 모델 또는 nullable-only relation target이 있어도 `godj generate` candidate 전체가 Go compile을 통과합니다.

기존 required relation selector, model binding validation, 오류 순서, generated role/file roster와 공개 API는 바뀌지 않습니다.

## 목표

- Required relation-query source/target과 무관한 project model 때문에 생성된 `_modelN` local이 unused가 되어 whole-candidate
  compile이 실패하는 경로를 회귀 테스트로 먼저 재현합니다.
- Required relation source가 존재하는 기존 mixed path에서는 모든 project model의 `orm.BindModel` 호출과 오류 검증을
  canonical identity order로 유지합니다. Scalar-only/no-required-source fast path는 계속 model local을 만들지 않습니다.
- 성공적으로 bind했지만 required relation-query assembly에서 사용하지 않는 model local만 `_ = _modelN`으로 명시적으로
  소비합니다.
- Existing all-used project golden과 current example generated bytes를 byte-identical하게 보존합니다.
- Scalar-only fast path와 combined sparse project 안의 required relation + unrelated scalar model, required relation +
  otherwise-unused nullable-only target 조건을 실제 외부 module compile로 검증합니다.

## 비목표

- Nullable ForeignKey typed relation-query selector 추가 또는 required-only surface 확대
- Unused model의 `BindModel` 생략, lazy binding, model filtering 또는 validation/error-order 변경
- Schema IR, Query AST, migration definition/state, backend/compiler와 transaction 변경
- Generated role/file roster, generator version, manifest ABI, public symbol 또는 facade 이름 변경
- Golden 일괄 재기준화, 현재 example bundle rewrite 또는 compatibility reset
- Form/Admin/API second-model 일반화, scaffold CLI, `sqlmigrate`, JWT/OAuth, Realtime
- 새로운 Django/DRF contract, deviation 또는 product aggregate status 변경

## 선행 조건과 기준 상태

- Clean baseline commit `2273df6d8f0d7c41a24dc65ba49b42e1be0885b8`, tree
  `941c546731da69c05ac404779c7536247fdbda10`에서 GDJ-0052/ADR-0054가 completed/Accepted입니다.
- Existing `GenerateProjectRelationQuery`는 required relation source가 하나라도 있으면 모든 project model을 bind하지만,
  return assembly와 `BindForward`가 참조하는 source/target 외 local은 별도로 소비하지 않습니다.
- `renderBindObjects`에는 같은 validation-preserving unused-binding pattern이 이미 존재합니다.
- Whole-candidate compile은 잘못된 generated source의 publication을 막으므로 last-known-good 손상은 없지만, valid public input의
  generation availability가 막힐 수 있습니다.
- Q-017은 broader reverse/general upgrade와 post-release upgrader 때문에 P1/open을 유지합니다.

## Django Reference / Contract

- 이 결함은 Django observable parity가 아니라 Go generated-source compile availability입니다. Django/Python source나 oracle을
  새로 실행하지 않습니다.
- 기존 GEN-M1-001의 deterministic generated source와 Q-017의 candidate-compile/publication 경계를 authority로 사용합니다.
- Passing 의미는 public-only external module의 candidate가 Go compile되고 existing golden/version/role ABI가 drift하지 않는 것입니다.

## 설계와 가설

`renderBindRelations`가 generation 시점에 다음 exact used set을 계산합니다.

```text
used model bind indexes
  = every required relation source
  ∪ every required relation target
```

모든 model은 기존처럼 canonical order로 `BindModel`하고 `_err`를 확인합니다. 그 뒤 used set에 없는 model에만
`_ = _modelN`을 생성합니다. 따라서 validation coverage는 축소되지 않고 Go compiler의 unused-local 규칙만 충족합니다.

Nullable relation은 현재 relation-query surface에서 의도적으로 제외되므로 그 target이 다른 required edge에 쓰이지 않으면 unused
binding입니다. 이를 required selector로 승격하지 않고 blank-consume하는 것이 이번 bounded fix입니다.

## 구현 단계

1. Activation/source audit — exact root cause와 byte/version/contract non-change를 고정합니다.
2. Regression first — existing direct external-compile fixture와 existing full-union fixture를 mixed
   required+unrelated-scalar+nullable-only-target shape로 확장하고 기존 source가 unused local로 실패하는지 확인합니다. 새 top-level
   또는 subtest identity는 추가하지 않습니다.
3. Minimal generator fix — used set을 만들고 successful unused binding만 blank-consume합니다.
4. Affected checkpoint — canonical/golden, external compile, project bundle, normal/race/CGO-disabled/vet/generated drift를 검증합니다.
5. Frozen integration — full local gate, Linux/386 compile-only, repository-external mixed bundle과 exact-head Hosted를 한 번 수행한 뒤
   terminal 상태 문서를 갱신합니다.

## 완료 조건

- [x] 기존 scalar-only generated project가 계속 compile되고 불필요한 model binding/import를 만들지 않음
- [x] required relation + unrelated scalar model candidate compile
- [x] required relation + otherwise-unused nullable-only target candidate compile
- [x] 모든 model의 BindModel/error observation과 canonical order 보존
- [x] existing all-used golden/current generated bundle byte identity와 generator version 보존
- [x] `codegen` normal/race/CGO-disabled/vet, project generation/compile tests와 `generate-check` 통과
- [ ] full frozen milestone, Linux/386 compile-only와 repository-external public-only mixed bundle 통과
- [ ] exact submitted-head Hosted와 독립 P0..P3 audit 통과
- [ ] work/CURRENT/evidence terminal 동기화; capability/Q-017/matrix status non-change 확인

## 진행 기록

- [x] 조사 — all-model bind와 required-edge-only use의 불일치를 확인
- [x] 설계 — validation-preserving used-set/blank-consume로 범위를 제한
- [x] 구현 — source `c7b3e6f...`, tree `e78692c...`
- [ ] 테스트 — affected checkpoint 통과; final frozen/Hosted pending
- [ ] 문서와 인수인계

## 수정 파일

- `codegen/project_relation_query.go`: used binding 판정과 unused local 소비
- `codegen/project_relation_query_test.go`: external compile 회귀와 byte/version 보존
- `codegen/project_bundle_test.go`: existing full-union fixture에 whole-candidate sparse-project 회귀 추가
- 상태/evidence 문서: 실제 checkpoint와 non-claim만 기록

## 결정된 사항

- 2026-08-31: Model binding validation은 availability fix 뒤에도 모든 model에 대해 유지합니다.
- 2026-08-31: Existing all-used bytes가 변하지 않으므로 generator version/role ABI를 올리지 않습니다.
- 2026-08-31: 새 public behavior나 장기 결정이 없어 별도 ADR/conformance contract를 만들지 않습니다.
- 2026-08-31: Regression은 existing direct compile/full-union test 본문과 기존 subtest roster만 확장하며 새 top-level/subtest
  identity를 추가하지 않습니다. Workflow inventory lock은 그대로 유지합니다.

## 미결정/Blocker

- 없음. 구현 중 existing all-used golden이나 role manifest가 바뀌면 이 가설을 폐기하고 범위를 재검토합니다.

## 테스트 증거

- Evidence IDs: EVID-20260831-168 activation; EVID-20260831-169 regression-first/affected checkpoint
- Command: exact red tests, focused/full `codegen` normal/race/CGO-disabled/vet, projectgenerate/compiletest, canonical count-10,
  `generate-check`, format/diff gates
- Result: fix 전 `_model1`/`_model2` unused compiler failure 재현; source `c7b3e6f...`에서 모든 affected gates 통과
- Not run: full `make ci`, Linux/386 all-package compile-only, frozen repository-external proof, exact-head Hosted

## 위험과 rollback

- Used set을 잘못 계산하면 필요한 binding을 blank-consume해도 runtime에는 영향이 없지만 generated byte drift를 만들 수 있습니다.
  Golden과 exact expected source fragment로 방지합니다.
- BindModel 전에 blank assignment를 만들거나 unused model bind를 생략하면 error precedence를 바꿀 수 있습니다. 반드시 successful
  error check 뒤에만 생성합니다.
- 현재 example/golden bytes가 바뀌면 generator version을 유지해서는 안 됩니다. 이 경우 변경을 되돌리고 새 packet/ABI 판단을
  먼저 합니다.
- Rollback은 이 packet의 codegen/test diff만 되돌리면 되며 schema/data migration은 없습니다.

## 다음 정확한 작업

Checkpoint 문서를 커밋한 뒤 source `c7b3e6f...`를 바꾸지 않고 full `make ci`, Linux/386 all-package compile-only와
repository-external public-only sparse bundle을 한 번 실행합니다. 통과한 frozen descendant를 push해 exact-head Hosted를 확인합니다.

## 결과와 인수인계

Source `c7b3e6f...`는 required source/target used set을 generation time에 계산하고 successful unused model local만 error check 뒤
blank-consume합니다. Existing direct external/full-union test identity를 sparse combined fixture로 확장했으며 기존 all-used golden,
current generated examples, public API, generator version, role/file ABI와 conformance aggregate는 바뀌지 않았습니다. 독립 code
audit의 P0/P1/P2는 0건이었고 combined-shape 표현 P3 한 건은 교정했습니다. Final frozen/Hosted acceptance는 아직 pending입니다.
