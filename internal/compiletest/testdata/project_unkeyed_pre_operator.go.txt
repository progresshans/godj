package consumer

import "github.com/progresshans/godj/project"

// This is the exact five-field positional shape that compiled before
// OpenSystemStateBackend and SystemOperatorPolicy became direct project.Config
// fields. The current-only pre-release source change is intentional and must
// remain observable rather than silently binding positions to a different
// contract.
var _ = project.Config{
	nil,
	nil,
	nil,
	nil,
	nil,
}
